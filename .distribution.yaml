# .distribution.yaml — Auto-generated by @suite/generator. Do not edit.
# Source: product.config.ts (gocertius) | Generated: 1970-01-01
# Schema: distribution_schema_version: 1 (MCP_Market_Distribution Phase C)

distribution_schema_version: 1

# Identity
reverse_dns_name: io.github.g-digital-by-Garrigues/gocertius
npm_scope: "@g-digital"
npm_package_name: "@g-digital/mcp-gocertius"
docker_image_name: gdigital/gocertius
n8n_adapter_target_name: n8n-nodes-gocertius

# Branding & metadata
license: MIT
credential_help_url: "https://www.gocertius.io"
logo_path: assets/logo-400x400.png

# Publication targets
target_overrides: {}
track_a_targets: default
track_b_targets: ['n8n']
skip_targets: []

git_tag_prefix: "v"

# Bundled Claude Code skills
bundled_skills:
  - getting-started.md
  - evidence-lifecycle.md
  - dossier-lifecycle.md
  - notification-lifecycle.md
  - chat-lifecycle.md
# Static tool inventory (39 tools — must equal toolsAllowList.length)
tools:
  - name: evidence_create
    description: "Registers a new evidence record inside an evidence group. Requires: evidence_group_create → evidenceGroupId, case_file_create → caseFileId. Generate a UUID v4 for &#x60;id&#x60;. Compute the SHA-256 hex hash of the file BEFORE calling. Returns uploadFileUrl (presigned S3 URL) — PUT the file bytes to that URL before calling evidence_seal. custodyType INTERNAL &#x3D; GoCertius stores the file; EXTERNAL &#x3D; only hash is registered."
  - name: evidence_list
    description: "Lists all evidence records in a specific evidence group. Use to review uploaded documents before sealing the group, or to find a specific evidenceId. Requires: caseFileId and evidenceGroupId. Returns paginated list with IDs, titles, status, and timestamps."
  - name: evidence_seal
    description: "Seals an evidence group and triggers qualified TSP timestamping. Requires: evidence_create records uploaded to S3, case_file_create → caseFileId, evidence_group_create → evidenceGroupId. Set evidencesCount to the number of evidences in the group. ASYNC: after calling, poll evidence_group_list until the group status changes to CLOSED before linking to a dossier or generating certificates."
  - name: evidence_get
    description: "Retrieves a specific evidence record. Requires: evidence_create → evidenceId, evidence_group_create → evidenceGroupId, case_file_create → caseFileId. Returns status (COMPLETED|IN_PROCESS|ERROR), hash, and tspTimestamp when certified."
  - name: evidence_group_create
    description: "Creates an evidence group inside a case file. Requires: case_file_create → caseFileId. Generate a UUID v4 for &#x60;id&#x60;. Set evidenceType to FILE, PHOTO, VIDEO, or WEB_PLUGIN. Returns evidenceGroupId. One group can contain multiple evidence records."
  - name: evidence_group_list
    description: "Lists all evidence groups in a case file with their current status (OPEN, CLOSING, CLOSED). Use to find an existing group or check which groups are ready for sealing. Requires: caseFileId."
  - name: dossier_create
    description: "Creates a dossier to aggregate certified evidence groups into a single tamper-evident PDF. Requires: case_file_create → caseFileId. Evidence groups must be in CLOSED status before linking. Generate a UUID v4 string for &#x60;id&#x60;. Returns dossierId. After creation, link evidence with dossier_evidence_link, then certify with dossier_certify."
  - name: dossier_update
    description: "Updates the metadata of an existing dossier (name, template fields, expiry). Requires: dossier_create → dossierId, caseFileId. Only available while dossier is in DRAFT status."
  - name: dossier_certify
    description: "Certifies a dossier, generating a tamper-evident PDF and locking all linked evidence. Requires: dossier_create → dossierId, dossier_evidence_link (evidence linked), case_file_create → caseFileId. ASYNC: transitions DRAFT → CERTIFYING → CERTIFIED. Poll dossier_list until dossierId status &#x3D;&#x3D;&#x3D; CERTIFIED before downloading."
  - name: dossier_list
    description: "Lists all dossiers in a case file with their status and metadata. Use to find an existing dossierId or monitor certification progress. Requires: caseFileId. Returns paginated list with IDs, names, status, and creation dates."
  - name: dossier_get
    description: "Retrieves the full details of a specific dossier including status, linked evidence, and download URLs. Use to check current state or get certificate download URLs after CERTIFIED. Requires: caseFileId and dossierId."
  - name: dossier_template_list
    description: "Lists available dossier templates. No prerequisites. Returns template IDs and their translations per language. Use the returned id as dossierTemplateId in dossier_create."
  - name: dossier_preview
    description: "Returns an HTML preview URL of a dossier before certification. Requires: caseFileId and dossierId."
  - name: dossier_document_url
    description: "Returns the download URL for the certified dossier PDF. Requires: dossier_certify (CERTIFIED status), caseFileId, dossierId."
  - name: dossier_package_url
    description: "Returns the download URL for the full dossier package (PDF + evidence files). Requires: dossier_certify (CERTIFIED status), caseFileId, dossierId."
  - name: dossier_visibility
    description: "Updates the visibility (public/private) of a certified dossier. Requires: dossier_certify (CERTIFIED status), caseFileId, dossierId."
  - name: dossier_delete
    description: "Deletes a dossier. Available in DRAFT status (to discard before certification) or in CERTIFIED status (to permanently remove the certified dossier). Irreversible. Requires: caseFileId and dossierId."
  - name: dossier_group_certify
    description: "Creates AND certifies a dossier from a single sealed evidence group in one call (express path). Requires: evidence_seal (CLOSED status), case_file_create → caseFileId, evidence_group_create → evidenceGroupId. Generate a UUID v4 string for &#x60;id&#x60;. Use when you have exactly one sealed evidence group and don&#x27;t need multi-group aggregation. Returns dossierId with CERTIFYING status → poll until CERTIFIED."
  - name: dossier_evidence_link
    description: "Links evidence items from a sealed group to a dossier. Requires: dossier_create → dossierId, evidence_seal (CLOSED), case_file_create → caseFileId. Pass the ids array of evidence UUIDs. Can be called multiple times for evidence from different case files."
  - name: dossier_evidence_list_to_link
    description: "Lists evidence items that are available to be linked to a dossier (CLOSED groups not yet linked). Requires: caseFileId and dossierId."
  - name: dossier_evidence_list
    description: "Lists all evidence items linked to a dossier. Requires: caseFileId and dossierId."
  - name: dossier_evidence_get
    description: "Retrieves details of a specific evidence item linked to a dossier. Requires: caseFileId, dossierId, evidenceId."
  - name: dossier_evidence_delete
    description: "Removes an evidence item from a dossier. Only available while dossier is in DRAFT status. Requires: caseFileId, dossierId, evidenceId."
  - name: notification_document_add
    description: "Performs the notification_document_add operation against the GoCertius API."
  - name: notification_request_create
    description: "Creates a certified notification request. Requires: case_file_create → caseFileId. Generate a UUID v4 for &#x60;id&#x60;. Set language to en_GB or es_ES. Returns notificationRequestId. Add at least one receiver with notification_receiver_add before sending. IMPORTANT: The &#x60;content&#x60; field must be valid HTML — plain text without HTML tags will not render on the recipient landing page. Only the following HTML formats are supported: paragraphs (&lt;p&gt;), bold (&lt;strong&gt;), italic (&lt;em&gt;), unordered lists (&lt;ul&gt;&lt;li&gt;), ordered lists (&lt;ol&gt;&lt;li&gt;). Do not use other HTML tags or CSS. Avoid special typographic characters (em dashes, smart quotes) in &#x60;subject&#x60;; use standard ASCII equivalents (hyphen, straight quotes) instead."
  - name: notification_request_send
    description: "Sends the certified notification to all added receivers. Requires: notification_request_create → notificationRequestId, notification_receiver_add (at least one receiver), case_file_create → caseFileId. ASYNC: triggers delivery. Poll notification_request_status until status is SENT or beyond (PARTIALLY_READ, FULLY_READ) before generating certificates."
  - name: notification_request_status
    description: "Checks the delivery status of a certified notification. Requires: notificationRequestId, caseFileId. Returns status (CREATING|DRAFT|IN_PROCESS|SENT|PARTIALLY_READ|FULLY_READ|PARTIALLY_ANSWERED|FULLY_ANSWERED). Poll until status is SENT or beyond. Do not call notification_certificate_get while status is CREATING, DRAFT, or IN_PROCESS."
  - name: notification_receiver_add
    description: "Adds a recipient to a notification request. Requires: notification_request_create → notificationRequestId, case_file_create → caseFileId. The &#x60;id&#x60; can be a UUID v4 or custom string (e.g. your internal user ID). Returns receiverId — save it for notification_certificate_get. Add all receivers before calling notification_request_send."
  - name: notification_certificate_get
    description: "Generates a PDF certificate for a specific receiver proving delivery and/or reading of the notification. Requires: notification_request_send (delivered), notification_receiver_add → receiverId, notification_request_create → notificationRequestId, case_file_create → caseFileId. Generate a UUID v4 for &#x60;id&#x60;. Returns pdfUrl when status reaches CERTIFIED."
  - name: case_file_create
    description: "Creates a new case file — the top-level container for all related operations (evidence, notifications, dossiers, chats). Call this first before any other operation. Generate a UUID v4 for &#x60;id&#x60;. Returns caseFileId needed for all subsequent calls."
  - name: case_file_list
    description: "Lists all case files in your GoCertius account. Pass userId (from session_login or session_info) to scope results to your account. Returns paginated list with IDs, names, and status."
  - name: case_file_get
    description: "Retrieves details of a specific case file. Requires: caseFileId. Use to verify a case file exists before creating evidence groups, dossiers, or notifications."
  - name: chat_create
    description: "Creates a certified chat channel (Telegram). IMPORTANT: Chats can only be created in the user&#x27;s personal case file (created automatically when the GoCertius account was opened). Do not use a manually created case file — use session_info → case_file_list to find the personal case file (oldest createdAt, owned by the user). Generate a UUID v4 for &#x60;id&#x60;. Set service to Telegram. Returns chatId. Use chat_invitation_url to get the shareable Telegram link."
  - name: chat_get
    description: "Retrieves details of a certified chat. Requires: case_file_create → caseFileId, chat_create → chatId. Returns status, participants, and registeredAt timestamp."
  - name: chat_invitation_url
    description: "Returns the Telegram invitation URL for a certified chat. Requires: chat_create → chatId, case_file_create → caseFileId. Share the returned invitationUrl with participants so they can join the certified channel."
  - name: chat_certificate_create
    description: "Creates a certificate of a range of messages from a certified chat. Requires: chat_create → chatId, case_file_create → caseFileId, messages already present in the Telegram channel. Generate a UUID v4 for &#x60;id&#x60;. Specify chatMessagesFrom and chatMessagesTo as ISO timestamps (chatMessagesFrom must be AFTER the chat registeredAt timestamp). ASYNC: poll chat_certificate_get until status &#x3D;&#x3D;&#x3D; CERTIFIED."
  - name: chat_certificate_get
    description: "Retrieves the certificate of a certified chat. Requires: chat_certificate_create → certificate id, chat_create → chatId, case_file_create → caseFileId. Returns documentUrl when status reaches CERTIFIED. Poll until CERTIFIED before using documentUrl."
  - name: session_login
    description: "Authenticates with GoCertius using email and password (or Azure AD device flow) to obtain a session JWT. Call this only if you encounter 401 errors. The MCP server manages authentication automatically. Example: { email: &#x27;user@example.com&#x27;, password: &#x27;...&#x27; }"
  - name: session_info
    description: "Retrieves information about the current authenticated session including userId, account, and token expiry. Use to verify who is authenticated or check session validity. No required parameters."
