# Auto-generated by @suite/generator. Do not edit. (Story 4.1, FR13/AR2)
# Ownership contract for this published repo:
#   generator:    emitted by the generator pipeline — never edit in this repo
#   distribution: produced by MCP_Market_Distribution (Phase B)
#   unlisted:     forbidden — remove or classify before publishing
# Entries ending in "/" are directory prefixes; a generator FILE entry inside
# a distribution DIRECTORY wins (e.g. .github/workflows/ci.yml).
# field_exceptions: FIELD-level ownership inside a generator-owned file. A CI
#   gate must allow the listed owner to write those fields — package.json is
#   generator-owned but Phase B writes its version.
artifact_owners_schema_version: 2
generator:
  - .artifact-owners.yaml
  - .claude/commands/
  - .claude/skills/
  - .distribution.yaml
  - .dockerignore
  - .env.example
  - .github/workflows/ci.yml
  - .gitignore
  - Dockerfile
  - LICENSE
  - ONBOARDING.md
  - README.template.md
  - assets/
  - docs/
  - package.json
  - scripts/
  - src/
  - tsconfig.json
  - tsup.config.ts
distribution:
  - .github/
  - README.md
  - environmentVariables.json
  - install-blocks/
  - n8n-node/
  - package-lock.json
  - server.json
  - smithery.yaml
field_exceptions:
  package.json:
    distribution:
      - version
unlisted: forbidden
