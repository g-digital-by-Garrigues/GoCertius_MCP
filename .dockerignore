# Auto-generated by @suite/generator. Do not edit.
# Build context for the Dockerfile's `COPY . .` build stage. That stage runs
# `npm ci` then `npm run build` (tsup), so it needs ONLY package.json,
# package-lock.json, tsconfig.json, tsup.config.ts and src/ — never exclude
# those five (npm ci fails outright without the lockfile).

# Reinstalled from scratch by `npm ci` in the build stage. Shipping the host's
# copy is dead weight at best and wrong-platform native binaries at worst.
node_modules/

# Rebuilt by `npm run build`; tsup runs with clean: true, and the runner stage
# copies dist/ from the build stage, never from the context.
dist/
.turbo/
*.tsbuildinfo

# Never read by npm ci or tsup. Keeping local credentials out of the `COPY . .`
# layer matters because a published image's intermediate layers can be exported
# to a cache tag — a secret in the build context does not stay private just
# because the final stage never copies it.
.env
.env.*

# Not read by the build; pure context weight.
.git/
.DS_Store
