// Auto-generated by @suite/generator. Do not edit.
// Sourced from operation: ListCaseFilesController_run (GET /users/{userId}/case-files)

import { defineTool } from "../core/index.js";
import { z } from "zod";
import { createClient, createConfig } from "../api/client/index.js";
import { listCaseFilesControllerRun } from "../api/sdk.gen.js";
import { zListCaseFilesControllerRunPath } from "../api/zod.gen.js";
import { zListCaseFilesControllerRunQuery } from "../api/zod.gen.js";
const inputSchema = z.object({
  ...zListCaseFilesControllerRunPath.shape,
  ...zListCaseFilesControllerRunQuery.shape,
});

export const case_file_list = defineTool({
  name: "case_file_list",
  description: "Performs the case_file_list operation against the GoCertius API. Review the API documentation for full field details.",
  inputSchema,
  annotations: {
    destructive: false,
    idempotent: true,
    requiresUserConfirmation: false,
  },
  pollable: false,
  idempotencyWindowSeconds: 60,
  async execute(input, ctx) {
    const token = ctx.auth?.token ?? "";
    const sdkClient = createClient(
      createConfig({
        baseUrl: process.env.MCP_API_BASE_URL ?? "",
        headers: { Authorization: `Bearer ${token}` },
      }),
    );

    // biome-ignore lint/suspicious/noExplicitAny: generated SDK call — input shape validated above
    const inp = input as any;
    // biome-ignore lint/suspicious/noExplicitAny: generated SDK function — types validated at generation time
    const sdkFn = listCaseFilesControllerRun as (opts: any) => Promise<any>;
    const response = await sdkFn({
      client: sdkClient,
      path: zListCaseFilesControllerRunPath.parse(inp),
      query: zListCaseFilesControllerRunQuery.parse(inp),
    });

    if (response.error !== undefined) {
      const msg = typeof response.error === "object" && response.error !== null && "message" in response.error
        ? String(response.error.message)
        : JSON.stringify(response.error);
      throw new Error(msg);
    }
    return response.data;
  },
});
