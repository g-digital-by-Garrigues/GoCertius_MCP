// Auto-generated by @suite/generator. Do not edit.
// Sourced from operation: CreateDossierController_run (POST /case-files/{caseFileId}/dossiers)

import { defineTool } from "../core/index.js";
import { z } from "zod";
import { createClient, createConfig } from "../api/client/index.js";
import { createDossierControllerRun } from "../api/sdk.gen.js";
import { zCreateDossierControllerRunPath } from "../api/zod.gen.js";
import { zCreateDossierControllerRunBody } from "../api/zod.gen.js";
const inputSchema = z.object({
  ...zCreateDossierControllerRunPath.shape,
  ...zCreateDossierControllerRunBody.shape,
});

export const dossier_create = defineTool({
  name: "dossier_create",
  description: "Performs the dossier_create operation against the GoCertius API. Review the API documentation for full field details.",
  inputSchema,
  annotations: {
    destructive: false,
    idempotent: false,
    requiresUserConfirmation: false,
  },
  pollable: false,
  idempotencyWindowSeconds: 60,
  async execute(input, ctx) {
    const token = ctx.auth?.token ?? "";
    const sdkClient = createClient(
      createConfig({
        baseUrl: process.env.MCP_API_BASE_URL ?? "",
        headers: {
          Authorization: `Bearer ${token}`,
          ...(ctx.correlationId ? { "X-Correlation-Id": ctx.correlationId } : {}),
        },
      }),
    );

    // biome-ignore lint/suspicious/noExplicitAny: generated SDK call — input shape validated above
    const inp = input as any;
    // biome-ignore lint/suspicious/noExplicitAny: generated SDK function — types validated at generation time
    const sdkFn = createDossierControllerRun as (opts: any) => Promise<any>;
    const response = await sdkFn({
      client: sdkClient,
      path: zCreateDossierControllerRunPath.parse(inp),
      body: zCreateDossierControllerRunBody.parse(inp),
    });

    if (response.error !== undefined) {
      const msg = typeof response.error === "object" && response.error !== null && "message" in response.error
        ? String(response.error.message)
        : JSON.stringify(response.error);
      throw new Error(msg);
    }
    return response.data;
  },
});
